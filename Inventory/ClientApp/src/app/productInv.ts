export interface ProDuctInv { // This is a new object we are inserting into the Item interface.
    id: number;
    productName: string;
    onHand: number;
    sales: number;
    itemId: string;
    locationID: string;
}